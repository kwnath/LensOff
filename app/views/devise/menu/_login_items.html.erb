<% if false %>
  This is commented code.

  Below is to create a new user session and destroy session.
  Method user_sign_in? is provided by devise.
<% end %>


<% if user_signed_in? %>
  <%= link_to('Logout', destroy_user_session_path, method: :delete) %>
<% else %>
  <%= link_to('Login', new_user_session_path)  %>
<% end %>
