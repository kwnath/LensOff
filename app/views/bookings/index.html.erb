<%= @bookings.each do |booking| %>
  <p>buyer:<%= booking.user.email %></p>
  <p>lens:<%= booking.lense.name %></p>
  <p>start_date:<%= booking.start_date %></p>
  <p>end_date:<%= booking.end_date %></p>
<% end %>
